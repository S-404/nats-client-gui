import React from 'react';
import iconAdd from '/icon-add.png';

import './addIcon.css';

const AddIcon = () => {
  return (
    <img className={'add-icon'} src={iconAdd} alt={'add'}/>
  );
};

export default AddIcon;
